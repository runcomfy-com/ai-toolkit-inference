# Inference Server - Inference Dependencies
# Install: pip install -r requirements-inference.txt

-r requirements.txt

# PyTorch - install with CUDA support first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
torch>=2.0.0
torchvision
torchaudio

torchao==0.10.0
safetensors
# Bumped 072d15ee (LTX-2.3, 2026-03-19) -> c943837 (2026-05-29) so the container
# has the Anima modular pipeline (huggingface/diffusers#13732). c943837 is the
# ai-toolkit training pin, already proven to host flux2/krea2/qwen_image/ltx2/
# anima together. This is a SHARED dependency for every pipeline -- regression
# test krea2/LTX-2.3/flux2/qwen_image on a GPU before releasing this bump.
git+https://github.com/huggingface/diffusers@c943837899b16cbae2f619b8dd4f7bb6f07dd81a
transformers==4.57.3
lycoris-lora==1.8.3
flatten_json
pyyaml
oyaml
tensorboard
kornia
invisible-watermark
einops
accelerate>=0.26.0
toml
albumentations==1.4.15
albucore==0.0.16
pydantic
omegaconf
k-diffusion
open_clip_torch
timm
prodigyopt
controlnet_aux==0.0.10
python-dotenv
bitsandbytes
hf_transfer
lpips
pytorch_fid
optimum-quanto==0.2.4
sentencepiece
huggingface_hub
peft
gradio
python-slugify
opencv-python
pytorch-wavelets==1.3.0
matplotlib==3.10.1
setuptools==69.5.1
scipy==1.12.0
av==16.0.1
torchcodec
